// Re-export types from the main types directory to avoid relative imports
export type {
  ExtendedUser,
  AuthMiddlewareContext,
  TypedSupabaseClient,
  AuthProvider,
  UserSettings,
} from "@/types/auth";
