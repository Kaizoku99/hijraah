'use client'; // Assuming chat needs client-side interactivity

import { UnifiedChatContainer } from '@/components/ui/unified-chat';

export default function ChatPage() {
    // Render the main chat interface component
    // Pass any necessary props if required by UnifiedChatContainer
    return <UnifiedChatContainer />;
} 